{
  "name": "oyenino-admin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create oyenino-forms-db",
    "db:migrate": "wrangler d1 execute oyenino-forms-db --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute oyenino-forms-db --local --file=schema.sql"
  }
}
